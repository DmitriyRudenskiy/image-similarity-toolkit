# image-similarity-toolkit
Image similarity toolkit with deep learning models (EfficientNet, ResNet, CLIP). Compute embeddings, compare images using cosine/Euclidean metrics, visualize results. Modular architecture with GPU acceleration, YAML config, CLI. Perfect for deduplication, recommendations, visual search. Production-ready.
